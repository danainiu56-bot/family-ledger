/**
 * 复制本文件为 config.js 并填入 Supabase 配置。
 * 与现有 H5（book/）使用同一个 Supabase 项目和 ledgers 表，即可共享同一份家人数据。
 *
 * 真机预览/体验版需在小程序后台「开发设置 → 服务器域名 → request 合法域名」
 * 添加 supabaseUrl 的域名（HTTPS）；开发者工具里可勾选「不校验合法域名」先调试。
 */
module.exports = {
  supabaseUrl: '',
  supabaseAnonKey: ''
};
